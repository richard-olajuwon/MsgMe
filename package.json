{
  "name": "msgme",
  "version": "1.0.0",
  "description": "",
  "main": "backend/server.js",
  "scripts": {
    "server-start": "cd backend && npm start",
    "server-install": "cd backend && npm install",
    "client-install": "cd frontend && npm install && npm run build",
    "socket-install": "cd socket && npm install",
    "socket": " cd socket && npm start",
    "install": "npm run server-install && npm run socket-install && npm run client-install",
    "start:prod": "concurrently \"npm run server-start\" \"npm run socket\""
  },
  "keywords": [],
  "author": "Richard",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
